// Video-2: Compiling typescript
const character = "harry";
console.log(character);

const inputs = document.querySelectorAll("input");

console.log(inputs);

inputs.forEach((input) => {
  console.log(input);
});

// Note: In order to compile typescript, it is necessary to convert it into javascript. This can be done by writing 'tsc filename.ts' in the terminal.